# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release
dist/

# mihomo 内核库（CI 编译/下载，不入库）
android/app/libs/*.aar
android/app/libs/*.jar
mihomo-bin/

# 离线分流数据（CI 构建时从 MetaCubeX/meta-rules-dat 下载，不在仓库里存；
# 本地开发用 tool/fetch_geodata.sh 获取）
assets/rules/*.dat
assets/rules/*.mmdb

# ---- iOS ----
# 内核框架 418MB，不入库（构建前由 tool/fetch_mihomo_ios.sh 下载）
ios/Mihomelib.xcframework/
# 扩展的 geo 数据来自 assets/rules（构建期下载），不重复入库
ios/PacketTunnel/country.mmdb
ios/PacketTunnel/geosite.dat
ios/build/
